source :rubygems
ruby '1.9.3'

gem 'thin'
gem 'rails'
gem 'mongoid', '~> 3.0.0'
gem 'jquery-rails'
gem 'json'
gem 'batman-rails', :git => 'https://github.com/Shopify/batman-rails.git'
gem 'haml-rails'
gem 'haml_assets'
gem 'active_model_serializers', :git => 'git://github.com/josevalim/active_model_serializers.git'
gem 'quiet_assets'
gem 'mongoid_auto_increment', :git => 'https://github.com/proton/mongoid_auto_increment.git'

group :development, :test do
end

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails'
  gem 'coffee-rails'
  gem 'uglifier'
  gem 'bootstrap-sass'
end

group :production do
end
